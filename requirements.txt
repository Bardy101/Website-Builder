# Postal Outreach Pipeline — Phase 0 (prospect finder) dependencies.
# See CLAUDE.md "Conventions" for the full intended stack.
requests>=2.28
PyYAML>=6.0

# Staleness detection (brief-visual-quality-signal.md part 1.1).
beautifulsoup4>=4.12

# Screenshot capture and contact-sheet thumbnails (part 2).
# After installing, run once:  playwright install chromium
playwright>=1.40
pillow>=10.0
